SELECT ELECTION_YEAR, COUNT(DISTINCT CANDIDATE), MAX(VOTES), SUM(VOTES) FROM ELECTION WHERE ELECTION_YEAR > 1850 GROUP BY ELECTION_YEAR HAVING COUNT(DISTINCT CANDIDATE) >= 2 AND MAX(VOTES) > 0.6 * SUM(VOTES);