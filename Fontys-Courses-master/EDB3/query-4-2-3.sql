SELECT LAST_NAME, HIRE_DATE, SALARY FROM EMPLOYEES e WHERE EXISTS (SELECT 'X' FROM EMPLOYEES WHERE SALARY = e.SALARY AND NVL(COMMISSION_PCT, 0) = NVL(e.COMMISSION_PCT, 0) AND LAST_NAME = 'Kochhar') AND LAST_NAME <> 'Kochhar';